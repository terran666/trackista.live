name: Deploy to Server

on:
  push:
    branches: [ main ]

permissions:
      id-token: write   # This is required for requesting the JWT
      contents: read    # This is required for actions/checkout

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm install
      
    - name: Build application
      run: npm run build
      
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          role-session-name: GitHub_to_AWS_via_FederatedOIDC
          aws-region: eu-central-1
        
    - name: Deploy to S3 (Ultra Aggressive Cache Busting)
      run: |
        echo "🧹 Ultra aggressive S3 bucket cleaning..."
        # Полная очистка S3 bucket
        aws s3 rm s3://trackista-live --recursive
        
        echo "⏳ Waiting for S3 cleanup to complete..."
        sleep 10
        
        echo "🚀 Deploying with timestamp-based cache busting..."
        TIMESTAMP=$(date +%s)
        
        # Синхронизируем все файлы заново с timestamp
        aws s3 sync dist/ s3://trackista-live --delete
        
        # Ultra aggressive no-cache для HTML файлов
        aws s3 cp dist/index.html s3://trackista-live/index.html \
          --cache-control "no-cache, no-store, must-revalidate, max-age=0, s-maxage=0, proxy-revalidate" \
          --expires "Thu, 01 Jan 1970 00:00:00 GMT" \
          --metadata-directive REPLACE \
          --metadata "timestamp=$TIMESTAMP"
        
        # Перезаписываем все assets с ultra aggressive no-cache
        aws s3 sync dist/assets/ s3://trackista-live/assets/ \
          --cache-control "no-cache, no-store, must-revalidate, max-age=0, s-maxage=0" \
          --expires "Thu, 01 Jan 1970 00:00:00 GMT" \
          --metadata-directive REPLACE
        
        echo "🔧 Configuring S3 for SPA routing with comprehensive setup..."
        
        # Проверяем, что файлы существуют
        echo "📋 Checking build files..."
        ls -la dist/
        
        if [ -f "dist/index.html" ]; then
          echo "✅ Found dist/index.html, setting up comprehensive SPA routing..."
          
          # 1. Создаем error.html для S3 website configuration
          echo "📄 Creating error.html..."
          aws s3 cp dist/index.html s3://trackista-live/error.html \
            --cache-control "no-cache, no-store, must-revalidate, max-age=0, s-maxage=0, proxy-revalidate" \
            --expires "Thu, 01 Jan 1970 00:00:00 GMT" \
            --metadata-directive REPLACE
            
          # 2. Создаем файлы без расширения для прямых URL (как nginx try_files)
          echo "� Creating direct route files..."
          aws s3 cp dist/index.html s3://trackista-live/density \
            --content-type "text/html" \
            --cache-control "no-cache, no-store, must-revalidate, max-age=0" \
            --metadata-directive REPLACE
          
          aws s3 cp dist/index.html s3://trackista-live/screener \
            --content-type "text/html" \
            --cache-control "no-cache, no-store, must-revalidate, max-age=0" \
            --metadata-directive REPLACE
            
          aws s3 cp dist/index.html s3://trackista-live/community \
            --content-type "text/html" \
            --cache-control "no-cache, no-store, must-revalidate, max-age=0" \
            --metadata-directive REPLACE
            
          aws s3 cp dist/index.html s3://trackista-live/charts \
            --content-type "text/html" \
            --cache-control "no-cache, no-store, must-revalidate, max-age=0" \
            --metadata-directive REPLACE
          
          # 3. Также создаем директории с index.html для полной совместимости
          echo "📁 Creating directory-based routes..."
          aws s3 cp dist/index.html s3://trackista-live/density/index.html \
            --cache-control "no-cache, no-store, must-revalidate, max-age=0" \
            --metadata-directive REPLACE
          
          aws s3 cp dist/index.html s3://trackista-live/screener/index.html \
            --cache-control "no-cache, no-store, must-revalidate, max-age=0" \
            --metadata-directive REPLACE
            
          aws s3 cp dist/index.html s3://trackista-live/community/index.html \
            --cache-control "no-cache, no-store, must-revalidate, max-age=0" \
            --metadata-directive REPLACE
            
          aws s3 cp dist/index.html s3://trackista-live/charts/index.html \
            --cache-control "no-cache, no-store, must-revalidate, max-age=0" \
            --metadata-directive REPLACE
            
        else
          echo "❌ dist/index.html not found! Build may have failed."
          exit 1
        fi
        
        # 4. Настраиваем S3 website configuration с routing rules
        echo "🌐 Configuring S3 website settings with routing rules..."
        
        # Создаем routing rules JSON для SPA
        echo '{"IndexDocument":{"Suffix":"index.html"},"ErrorDocument":{"Key":"index.html"},"RoutingRules":[{"Condition":{"HttpErrorCodeReturnedEquals":"404"},"Redirect":{"ReplaceKeyWith":"index.html"}}]}' > routing-rules.json
        
        # Применяем website конфигурацию с routing rules
        aws s3api put-bucket-website --bucket trackista-live --website-configuration file://routing-rules.json
        
        # Также устанавливаем простую website конфигурацию как fallback
        aws s3 website s3://trackista-live --index-document index.html --error-document index.html
        
        # 5. Проверяем что все файлы загружены
        echo "🔍 Verifying uploaded files..."
        aws s3 ls s3://trackista-live/ --human-readable
        
        # 6. Проверяем website конфигурацию
        echo "🔍 Verifying S3 website configuration..."
        aws s3api get-bucket-website --bucket trackista-live
        
        echo "✅ Deployment completed with SPA routing support. Timestamp: $TIMESTAMP"